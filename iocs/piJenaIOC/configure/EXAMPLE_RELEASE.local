# Use the following lines if motorPiJena was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorPiJena was built outside motor
#!MOTOR_PIJENA=
#!-include $(MOTOR_PIJENA)/configure/RELEASE.local
